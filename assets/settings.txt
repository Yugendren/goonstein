# Personal defaults, read at startup. Command-line flags override these.
volume 0.8       # game sound 0..1 (0 = mute). Headless test runs always pass --volume 0
debug 0         # 1 starts with the F1 overlay on
# hero goon_a   # override the local player's character; commented out so each net slot
                # plays its own goon (slot 0..3 = goon_a..goon_d)
fps 0           # frame cap: 0 = your display's rate, or 30 60 90 120 144 240
vsync 1         # 0 lets the frame rate exceed the display (tearing possible)
level island    # the level to start in (assets/levels/NAME.txt)
quality normal  # potato | normal | high (--quality overrides). Delete this line and the game
                # guesses from your cores/RAM, then probes two seconds of real play and writes
                # back whatever it decided (see src/quality.c)
# --- menu --- the front door remembers these for you; the menu writes them back when you change them
name goon                  # your name in the player list and in join messages
port 7777                  # the UDP port HOST GAME listens on
last_join 127.0.0.1:7777   # the address JOIN GAME offers first

mouse_sens 1.0             # mouse look sensitivity: multiplies the default 0.0022 radians per mouse pixel
sprint hold                # hold = Shift runs while you hold it (the default, and what every shooter does)
                           # toggle = Shift switches the run on and off; it switches itself off when you stop

# voice: ptt (hold V) | open (mic always on, energy gate) | off
voice ptt
voice_volume 1.0           # how loud other players are, independent of volume
voice_monitor 0            # 1 = hear your own changed voice (solo testing)
