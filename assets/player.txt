# Player tuning. Times in seconds (60 Hz sim: 0.15 s = 9 frames).
hp 100
speed 3.2
sprint_mult 1.56
turn_speed 16

# Ground movement (Half-Life/Quake shaped): walk 3.2 -> jog 5.0 on sprint (speed * sprint_mult).
# accel/air_accel/friction are Quake's sv_accelerate/sv_friction-shaped coefficients, not m/s^2 --
# see the friction/accel math in player_update (combat.c). jump_height is metres, converted to a
# launch vy against game.c's gravity. crouch_mult only scales speed here; the eye height and stance
# drop are game.c's job.
accel 10
air_accel 10
friction 8
stop_speed 1.4
jump_height 1.0
crouch_mult 0.5

# Swings: the windup here is only a fallback. When the hero model's attack clips carry a contact
# mark the fight takes the windup off the animation instead, so the hit lands with the blade.
# active/recovery/damage are per-swing multiples of these (see SWING_* in combat.c).
attack_windup 0.16
attack_active 0.11
attack_recovery 0.32
attack_damage 6
attack_range 1.9
attack_posture 7

# Guard. Tap right mouse to deflect inside parry_window, hold it to block.
parry_window 0.14
parry_recovery 0.34   # a whiffed deflect leaves you open
parry_hitstop 0.12

dodge_time 0.42
dodge_iframes 0.28
dodge_dist 3.2
hurt_time 0.42

# Posture: deflecting is nearly free, blocking is not. At zero the guard breaks and the boss
# gets a free hit. Regen pauses for posture_delay seconds after anything lands.
posture 100
posture_regen 26
posture_delay 0.9
block_posture 0.85    # posture spent blocking a hit, as a fraction of its damage
deflect_posture 4
stagger_time 1.5
knockback 2.2

size 0.5 1.8 0.5
color 0.6 0.55 0.5
