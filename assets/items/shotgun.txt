# shotgun.txt -- the loud one, found near the boat's mooring line. Two barrels' arguing.
# Kenney Blaster Kit's blaster-f (models/kenney/blaster-kit/shotgun.glb): the kit's longest single
# design (main body plus small scope and magazine sub-meshes), picked over the compact ones for a
# "biggest, loudest gun in the box" silhouette. Raw model bounds are 0.17 x 0.37 x 1.31 m
# (X x Y x Z, aggregated over all three sub-meshes), origin at the model's own geometric centre on
# X and Z; Y is centred to within 5 mm, near enough to ignore. Scale 0.76 brings it to a long-gun
# 1.0 m; half below is half of the scaled bounds, with the Y half taken from the larger of the
# model's +Y/-Y reach so the box does not clip the scope or the magazine. NOTE for hand/viewmodel
# attachment: the muzzle points -Z, not +Z -- the grip is not at the origin either, it is a cluster
# of geometry around z=+0.29 (0.38 raw, offset from centre); see ASSETS.md.
model      models/kenney/blaster-kit/shotgun.glb
display    Shotgun, Duck Season Unclear
weapon     gun
damage     100                         # of a 100-point wind pool; puts a goon on the floor
rate       0.9                         # shots per second
range      18                          # metres; hitscan
ammo       6                           # rounds in a full load
pellets    6                           # hitscan rays per shot, on a fixed fan; a spread, not a bullet
knock      9.0                         # m/s shove on whatever it hits
fire_sound boom                        # firing sound
mass       3.6                         # kg
half       0.065 0.144 0.498           # half the scaled model bounds (0.129 x 0.288 x 0.996 m)
fragile    0                           # unbreakable
value      0                           # dollars; not evidence of anything
grip       0 0.030 0.270   180 0 0        # same kit, same flip; the grip sits well back on a long gun
two_handed no
scale      0.76
tint       1.0 1.0 1.0                 # the model carries its own (toy-bright) paint
